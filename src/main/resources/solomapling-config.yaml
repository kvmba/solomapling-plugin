# Deprecated placeholder — see soloMapling/Environment/CONFIG.md

# Dialogue / script language (en-US | zh-CN). When unset, falls back to gms.service.language.
# language: zh-CN

# Population config lives in soloMapling/Environment/EnvironmentPopulation.yaml
