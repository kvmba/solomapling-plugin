target/
.idea/
*.iml
.classpath
.project
.settings/
.DS_Store
*.class
*.log
.mvn/wrapper/maven-wrapper.jar

# Local translation / audit scratch
scripts/.zh_translate_cache*.json
scripts/zh_todo_strings.json

# Generated by scripts/validate_bot_config_ids.py on every run (overwritten, not
# read by anything). Committed once by accident; keep it out of the tree so a
# local run cannot wipe or churn the tracked copy.
scripts/bot_config_character_ids.txt

# CodeGraph local index (generated by `codegraph init`)
.codegraph/

# Runtime log output (log4j2 writes logs/ into the server working dir)
logs/
